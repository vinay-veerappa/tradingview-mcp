<<<VERDICT>>>
REVISE
<<<END VERDICT>>>
<<<FINDINGS>>>
- [BLOCKER] src/gateway/http.js: `getState({ _deps })` passes `_deps` inside an options object, but `paneScan(_deps)` passes it as a bare positional argument. If `getState` expects `{ _deps }` while `paneScan` expects `deps` directly, one of these two calls is wrong. The concrete failure: whichever signature is mismatched will see `_deps` as `undefined` inside the core function, so the offline test's `OFFLINE_DEPS` never reaches the real CDP call — the handler bypasses the test seam and attempts a real browser connection, hanging the test or, in production, ignoring dependency injection.
- [BLOCKER] src/gateway/http.js: `const data = await route.handler(req, url, _deps);` — the `ROUTES` array entries are defined as arrow functions that *immediately invoke* the core function (e.g., `(_req, _url, _deps) => getState({ _deps })`). These execute at module load time, not at request time. The concrete failure: `getState({ _deps })` runs once with `_deps = undefined` when the module is imported, producing a promise that is never awaited or returned. Every `/state` request gets `undefined` from the route handler (the arrow returns the promise from the already-settled call, or `undefined` if the call threw synchronously), so `res.end(JSON.stringify(data ?? { success: true }))` always writes `{ success: true }` with no actual state data.
- [MAJOR] src/core/context.js: `signal.addEventListener('abort', () => rej(abortError), { once: true });` — when the op wins the race (completes before abort), the abort listener is never removed. The concrete failure: a long-lived `AbortSignal` (e.g., an MCP session-level signal) accumulates a listener on every `withChartContext` call. Over a session of 1000 snapshot calls, 1000 closures pin `rej` and the `opP` promise graph in memory — a slow leak that also triggers Node's `MaxListenersExceededWarning` after 11 calls.
- [MAJOR] src/core/context.js: When `signal` aborts and `abortP` wins the race, `opP` is still pending and will eventually settle. The restore path runs (correctly), but `opP`'s eventual resolution/rejection is unhandled. If `op` rejects (e.g., page-side evaluate throws), the rejection propagates as an unhandled rejection, potentially crashing the process. The concrete failure: caller aborts a snapshot; the page-side evaluate fails concurrently; an `unhandledRejection` event fires and, under default Node 15+ semantics, terminates the process.
- [MAJOR] src/gateway/http.js: `res.on('close', () => { disconnected.stop = true; });` fires after `sseHead` has already written headers. If the client disconnects before `sseHead` (between TCP accept and route dispatch), `req.on('close')` fires but `res` may not be writable — the subsequent `res.write` in the loop throws, which is caught, but `res.end()` in the finally block throws into the void. More critically, if `subscribe()` is a long-running generator that doesn't check `shouldStop` frequently, the loop doesn't break until the next yield. The concrete failure: client connects to `/stream/quote` and immediately disconnects; `subscribe` is mid-`evaluate` (a 5-second CDP round-trip); the poll loop doesn't check `disconnected.stop` until `evaluate` returns, so the subscriber runs for 5 seconds against a dead socket, and `res.write` throws on each attempt.
- [MAJOR] src/gateway/http.js: `closeServer` calls `stopAll()` then `server.close()`, but `server.close()` only stops accepting *new* connections — it does not close existing ones. `stopAll()` destroys sockets, but SSE handlers catching the destroy will try `res.end()` on a destroyed socket. The concrete failure: `close()` is called (e.g., SIGINT); `stopAll` destroys all connections; each SSE handler's `for await` loop catches the error, then `try { res.end(); }` throws `ERR_STREAM_DESTROYED`, which is caught — but the `for await` generator from `subscribe()` may still be alive if `shouldStop` wasn't checked before the destroy. The subscriber poll loop becomes an orphan until the next `shouldStop` check.
- [MINOR] src/gateway/http.js: `import { listCapabilities } from '../capabilities.js';` — the path `../capabilities.js` resolves to `src/capabilities.js` from `src/gateway/http.js`. If the file is actually at `src/core/capabilities.js` or has a different export name, this is a compile break. Cannot verify without the file listing, but the import path is suspicious given other core imports use `../core/`.
- [MINOR] src/core/health.js: `const repoRoot = dirname(dirname(dirname(fileURLToPath(import.meta.url))));` — `health.js` is at `src/core/health.js`, so `dirname` three times gives `src/` → repo root → parent of repo root. The concrete failure: `git rev-parse HEAD` runs in the *parent* directory of the repo, which may not be a git repository at all, causing `execSync` to throw and the update check to silently fail (caught by the outer `try`). The fix should be `dirname(dirname(fileURLToPath(import.meta.url)))` (two levels, not three) to reach the repo root from `src/core/`.
<<<END FINDINGS>>>
<<<REQUIRED>>>
- Fix the ROUTES table so handlers are functions that defer execution until called, not arrows that execute at module load. Change each entry to return a thunk: e.g., `{ method: 'GET', path: '/state', handler: (_req, _url, _deps) => getState({ _deps }) }` is correct ONLY if the arrow body is not invoked at definition time — verify that `getState({ _deps })` is NOT called during array construction. If the current code calls the core function at array-literal time, wrap each in a thunk: `handler: (_req, _url, _deps) => { return getState({ _deps }); }`.
- Verify and fix the `_deps` threading for `getState` vs `paneScan`: read the actual signature of each. If `getState` takes `{ _deps }` and `paneScan` takes `_deps` positionally, the current code is correct. If not, fix the mismatched call. Add a test that asserts the offline deps object reaches the core function (e.g., spy on `evaluate` call count).
- In `withChartContext`, after the `Promise.race`, if `opP` is still pending and abort won, attach `.catch(() => {})` to `opP` to prevent unhandled rejection: `opP.catch(() => {});` before or after the race.
- Remove the abort listener after the race resolves to prevent the listener leak: capture the listener function and call `signal.removeEventListener('abort', listener)` in a `.finally()` on the race promise.
- In `health.js`, fix the `repoRoot` computation: use `dirname(dirname(fileURLToPath(import.meta.url)))` (two `dirname` calls, not three) to go from `src/core/health.js` to the repo root.
- In `handleStream`, check `disconnected.stop` immediately before each `res.write` and break before writing to a potentially closed socket. Also wrap `res.write` in try/catch to handle `ERR_STREAM_DESTROYED` gracefully without relying on the outer catch.
- Verify `import { listCapabilities } from '../capabilities.js'` resolves correctly; if the file is under `src/core/`, fix the path to `../core/capabilities.js`.
<<<END REQUIRED>>>