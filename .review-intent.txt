Review the post-v3 gateway work in tradingview-mcp: loopback HTTP gateway exposing a read surface plus SSE streams over the subscribe() primitive, and an AbortSignal cancellation slice through withChartContext/session_snapshot. Watch specifically for: (1) security — the gateway binds loopback only, is read-only (mutations must 405), no accidental command/exec routes, no secrets in responses; (2) resource leaks — SSE subscriber poll loops must terminate on disconnect/close, no orphaned timers or open sockets in tests; (3) cancellation correctness — abort rejects caller while restore still runs; pre-aborted signals never touch the chart; no race where abortError is read before assignment; (4) signature drift like the earlier notify(server, uri) bug — verify _deps threading matches each core function's actual signature (getState takes {_deps}, paneScan takes deps object directly); (5) error envelope contract — codes/retryable/outcome_unknown are preserved through the HTTP layer; (6) test honesty — environment-dependent assertions (CDP up vs down) are not flaky; CI on a machine without TV must pass. Files: src/gateway/http.js, src/gateway (all), src/core/context.js, src/core/snapshot.js, src/tools/snapshot.js, src/cli/commands/gateway.js, src/cli/index.js, src/core/health.js, tests/gateway.test.js, tests/context.test.js, package.json.